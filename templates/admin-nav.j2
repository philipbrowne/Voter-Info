{% if 'username' in session %}
    {% if user.is_admin == True %}
        <nav class="navbar fixed-bottom navbar-expand-lg navbar-dark">
            <div class="container-fluid" id="navbarcontainer">
                <a class="navbar-brand brand-text" href="/">Admin Toolbar</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                    aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav">
                        <ul class="navbar-nav">
                            <li class="nav-item">
                                <a class="nav-link text-light " href="/admin/users">Users</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link text-light " href="/admin/states">States</a>
                            </li>
                        </ul>
                </div>
        </nav>
    {% endif %}
{% endif %}